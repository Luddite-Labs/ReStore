<Window x:Class="ReStore.Views.Windows.RestoreConfirmWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework" Title="Confirm Restore" Height="620" Width="760" WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="NoResize" TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType" Background="{DynamicResource ApplicationBackgroundBrush}" UseLayoutRounding="True">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="32" CornerRadius="8" GlassFrameThickness="0" ResizeBorderThickness="0"/>
    </shell:WindowChrome.WindowChrome>

    <Border BorderThickness="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="{DynamicResource ApplicationBackgroundBrush}" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <TextBlock Text="Confirm Restore" VerticalAlignment="Center" Margin="16,0" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            </Grid>

            <Border Grid.Row="1" Margin="24,24,24,0" Padding="16" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                <StackPanel>
                    <TextBlock x:Name="SummaryText" FontSize="15" FontWeight="SemiBold" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                    <TextBlock x:Name="TargetText" FontSize="12" FontFamily="Consolas" TextWrapping="Wrap" Margin="0,6,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Name="ConflictWarningText" FontSize="13" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,10,0,0" Foreground="#EF4444" Visibility="Collapsed"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="2" Margin="24,12,24,0" Padding="16" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="If a file already exists at the destination" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                    <RadioButton x:Name="SkipRadio" GroupName="Conflict" IsChecked="True" Margin="0,0,0,4" Content="Skip it - keep the file that is already there (recommended)" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                    <RadioButton x:Name="KeepBothRadio" GroupName="Conflict" Margin="0,0,0,4" Content="Keep both - restore alongside as 'name (restored).ext'" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                    <RadioButton x:Name="OverwriteRadio" GroupName="Conflict" Content="Overwrite - replace the existing file with the backup copy" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="3" Margin="24,12,24,0" Padding="16" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                <DockPanel>
                    <Grid DockPanel.Dock="Top" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" x:Name="SelectionText" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                        <Button Grid.Column="1" x:Name="SelectAllBtn" Content="Select All" Padding="10,4" Margin="0,0,8,0" Click="SelectAllBtn_Click"/>
                        <Button Grid.Column="2" x:Name="DeselectAllBtn" Content="Deselect All" Padding="10,4" Click="DeselectAllBtn_Click"/>
                    </Grid>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="FileList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="4" Padding="8,6" Margin="0,0,0,4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <TextBlock Grid.Column="1" Text="{Binding RelativePath}" FontSize="12" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                            <TextBlock Grid.Column="2" Text="{Binding SizeLabel}" FontSize="11" VerticalAlignment="Center" Margin="10,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                            <Border Grid.Column="3" Background="{Binding ConflictColor}" CornerRadius="3" Padding="6,2" VerticalAlignment="Center" Visibility="{Binding ConflictVisibility}">
                                                <TextBlock Text="{Binding ConflictLabel}" FontSize="10" FontWeight="SemiBold" Foreground="White"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <Border Grid.Row="4" Margin="0,16,0,0" Padding="24,16" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CancelBtn" Content="Cancel" Padding="16,6" Margin="0,0,8,0" Click="CancelBtn_Click"/>
                    <Button x:Name="RestoreBtn" Content="Restore" Padding="16,6" Click="RestoreBtn_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
